const Popup = () => {
    return ( 
        <div className=" "></div>//this div will be centered on the sreen and will display, if the answer matches with that of the mode
        //initially the model will have visibility set as none then on submit the popup will pop
     );
}
 
export default Popup;