//create an array of path in play.js

//generate a random number from 0 to length-1 to get the element 

//useRandomImage takes in an array and sets the image path randomly
const useRandomImage = (array) => {
    
}
 
export default useRandomImage 